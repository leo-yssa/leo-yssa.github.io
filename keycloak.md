                    ┌─────────────────────────────────────────────────────────┐
                    │ Keycloak (인증 서버)                                      │
                    │  - 로그인 UI / ID·PW, 소셜 브로커(Kakao, Google)          │
                    │  - access_token, refresh_token 발급                        │
                    │  - JWKS로 서명 공개키 제공                                 │
                    └─────────────────────────────────────────────────────────┘
                                         │
         로그인 요청                      │ 토큰 발급
         (리다이렉트 또는                 │
          프론트에서 Keycloak 호출)        ▼
                                    ┌─────────┐
                                    │ 클라이언트 │
                                    └─────────┘
                                         │
         API 요청 + Bearer access_token   │
                                         ▼
                    ┌─────────────────────────────────────────────────────────┐
                    │ 이 NestJS 앱 (리소스 서버)                                │
                    │  - Keycloak JWT 검증 (JWKS 또는 OIDC 설정)                 │
                    │  - payload → AuthenticatedUser (sub, roles 등 매핑)      │
                    │  - 기존 Guard/역할/권한 로직 재사용 가능                    │
                    └─────────────────────────────────────────────────────────┘